apiVersion: v1
kind: Service
metadata:
  name: docker-login-test
  namespace: test-webhooks
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: pullsecretvalidationcert
spec:
  selector:
    app: docker-login-test
  ports:
    - protocol: TCP
      port: 443
      targetPort: 8080
