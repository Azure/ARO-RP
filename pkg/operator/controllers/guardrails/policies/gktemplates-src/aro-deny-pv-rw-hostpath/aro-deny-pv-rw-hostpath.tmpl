apiVersion: templates.gatekeeper.sh/v1
kind: ConstraintTemplate
metadata:
  name: arodenypvrwhostpath
  annotations:
    metadata.gatekeeper.sh/title: "Persistent Volume sHost Mount"
    metadata.gatekeeper.sh/version: 1.0.0
    description: >-
      To prevent the creation of PersistentVolume with read/write AccessModes.
spec:
  crd:
    spec:
      names:
        kind: ARODenyPVRWHostPath
      validation:
        # Schema for the `parameters` field
        openAPIV3Schema:
          type: object
          description: >-
            To prevent the creation of PersistentVolume with read/write AccessModes.
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
{{ file.Read "gktemplates-src/aro-deny-pv-rw-hostpath/src.rego" | strings.Indent 8 | strings.TrimSuffix "\n" }}
      libs:
        - |
{{ file.Read "gktemplates-src/library/common.rego" | strings.Indent 10 | strings.TrimSuffix "\n" }}
