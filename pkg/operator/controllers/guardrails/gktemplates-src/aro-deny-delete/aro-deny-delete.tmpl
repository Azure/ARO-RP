apiVersion: templates.gatekeeper.sh/v1
kind: ConstraintTemplate
metadata:
  name: arodenydelete
  annotations:
    description: >-
      Prevents unexpected deletion of a specific critical resource
spec:
  crd:
    spec:
      names:
        kind: ARODenyDelete
      validation:
        openAPIV3Schema:
          type: object
          properties:
            name:
              type: string
              description: >-
                The specific resource name that is not allowed to be deleted.
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
{{ file.Read "gktemplates-src/aro-deny-delete/src.rego" | strings.Indent 8 | strings.TrimSuffix "\n" }}
