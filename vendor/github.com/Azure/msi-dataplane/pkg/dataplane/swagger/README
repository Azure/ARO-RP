# Overview

This package is a client for interacting with MSI-RP dataplane.

# Swagger File

The client is built via the [swagger file](https://msazure.visualstudio.com/One/_git/ManagedIdentity-MIRP?path=/src/Product/MSI/swagger/CredentialsDataPlane&version=GBmaster) from MSI team. We use API version `2024-01-01`.

# Code Generation
To create the `zz_generated_*` files, run `make generate` from the root of this repository.