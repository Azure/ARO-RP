parameters:
  workingDirectory: ""
steps:
  - script: |
      cd ${{ parameters.workingDirectory }}

      . secrets/env
      . ./hack/e2e/run-rp-and-e2e.sh

      if [ $RP_MODE = "development" ] 
      then
        kill_rp
      else
        echo "### skipping killing rp because not development rp"
      fi
    displayName: "🚀 Run ARO RP & E2E"
