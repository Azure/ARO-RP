apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimo.fullname" . }}
  labels:
    {{- include "mimo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mimo.selectorLabels" . | nindent 4 }}
