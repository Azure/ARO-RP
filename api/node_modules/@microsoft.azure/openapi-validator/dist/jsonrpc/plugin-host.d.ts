import { Mapping, Message, RawSourceMap } from "./types";
export interface IAutoRestPluginInitiator {
    ReadFile(filename: string): Promise<string>;
    GetValue(key: string): Promise<any>;
    ListInputs(): Promise<string[]>;
    WriteFile(filename: string, content: string, sourceMap?: Mapping[] | RawSourceMap): void;
    Message(message: Message): void;
}
declare type AutoRestPluginHandler = (initiator: IAutoRestPluginInitiator) => Promise<void>;
export declare class AutoRestPluginHost {
    private readonly plugins;
    Add(name: string, handler: AutoRestPluginHandler): void;
    Run(): Promise<void>;
}
export {};
