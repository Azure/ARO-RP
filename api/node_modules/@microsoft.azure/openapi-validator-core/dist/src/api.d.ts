import { IRuleSet, LintResultMessage, OpenApiTypes, IFileSystem } from "./types";
export declare type LintOptions = {
    ruleSet: IRuleSet;
    openapiType: OpenApiTypes;
    fileSystem?: IFileSystem;
};
export declare type LintCallBack = (msg: LintResultMessage) => void;
export declare function lint(swaggerPaths: string[], options: LintOptions, cb?: LintCallBack): Promise<LintResultMessage[]>;
export declare function LintTester(sampleFilePath: string | string[], ruleSet: IRuleSet, ruleName?: string, fileSystem?: IFileSystem): Promise<LintResultMessage[]>;
