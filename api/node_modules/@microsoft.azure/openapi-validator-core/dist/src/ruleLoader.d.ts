import { IRuleSet } from "./types";
export interface IRuleLoader {
    getRuleSet: (rulesetPath?: string) => IRuleSet | Promise<IRuleSet>;
}
export declare class BuiltInRuleLoader {
    getRuleSet(): IRuleSet;
}
