export declare class Resolver {
    private innerDoc;
    private currentFile;
    private references;
    constructor(innerDoc: any, currentFile: string);
    resolve(): Promise<void>;
    getReferences(): string[];
}
