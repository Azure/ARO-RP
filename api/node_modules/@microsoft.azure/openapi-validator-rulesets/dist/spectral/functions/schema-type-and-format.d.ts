import type { IFunctionResult } from "@stoplight/spectral-core";
import type { JsonPath } from "@stoplight/types";
declare type Oas2Schema = {
    type: string;
    format: string;
    properties: {
        [key: string]: Oas2Schema;
    };
    allOf: Oas2Schema[];
};
declare function checkSchemaTypeAndFormat(schema: Oas2Schema, options: any, { path }: {
    path: JsonPath;
}): IFunctionResult[];
export default checkSchemaTypeAndFormat;
