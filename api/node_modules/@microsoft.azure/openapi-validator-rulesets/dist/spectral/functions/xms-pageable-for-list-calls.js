"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const lodash_1 = require("lodash");
const rules_helper_1 = require("../../native/utilities/rules-helper");
const xmsPageableForListCalls = (swaggerObj, _opts, paths) => {
    if (swaggerObj === null) {
        return [];
    }
    const path = paths.path || [];
    if (!(0, lodash_1.isNull)(path[1])) {
        if (!(0, rules_helper_1.isListOperationPath)(path[1].toString())) {
            return;
        }
    }
    if (swaggerObj["x-ms-pageable"])
        return [];
    else
        return [
            {
                message: "`x-ms-pageable` extension must be specified for LIST APIs.",
                path: path,
            },
        ];
};
exports.default = xmsPageableForListCalls;
//# sourceMappingURL=xms-pageable-for-list-calls.js.map