"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
function operationsApiSchema(schema, options, { path }) {
    if (schema === null || typeof schema !== "object") {
        return [];
    }
    const errors = [];
    let isValid = true;
    const value = (0, utils_1.getProperty)(schema, "value");
    const items = value === null || value === void 0 ? void 0 : value.items;
    if (value && items) {
        const name = (0, utils_1.getProperty)(items, "name");
        const display = (0, utils_1.getProperty)(items, "display");
        const isDataAction = (0, utils_1.getProperty)(items, "isDataAction");
        if (!name || !isDataAction || !display) {
            isValid = false;
        }
        else {
            if (["description", "provider", "operation", "resource"].some((e) => !(0, utils_1.getProperty)(display, e))) {
                isValid = false;
            }
        }
    }
    else {
        isValid = false;
    }
    if (!isValid) {
        errors.push({
            message: path[1],
            path,
        });
    }
    return errors;
}
exports.default = operationsApiSchema;
//# sourceMappingURL=operations-api-schema.js.map