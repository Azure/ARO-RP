"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
const putGetPatchSchema = (pathItem, opts, ctx) => {
    if (pathItem === null || typeof pathItem !== "object") {
        return [];
    }
    const neededHttpVerbs = ["put", "get", "patch"];
    const path = ctx.path || [];
    const errors = [];
    const models = new Set();
    for (const verb of neededHttpVerbs) {
        if (pathItem[verb]) {
            models.add((0, utils_1.getReturnedType)(pathItem[verb]));
        }
        if (models.size > 1) {
            errors.push({
                message: "",
                path,
            });
            break;
        }
    }
    return errors;
};
exports.default = putGetPatchSchema;
//# sourceMappingURL=put-get-patch-schema.js.map