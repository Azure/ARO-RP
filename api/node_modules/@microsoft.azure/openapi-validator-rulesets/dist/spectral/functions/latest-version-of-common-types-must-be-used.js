"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.latestVersionOfCommonTypesMustBeUsed = void 0;
const utils_1 = require("./utils");
const latestVersionOfCommonTypesMustBeUsed = (ref, _opts, ctx) => {
    const REF_COMMON_TYPES_REGEX = new RegExp("/common-types/resource-management/v\\d+/\\w+.json#", "gi");
    if (ref === null || !ref.match(REF_COMMON_TYPES_REGEX)) {
        return [];
    }
    const errors = [];
    const path = ctx.path;
    const versionAndFile = ref.split("/common-types/resource-management/")[1].split("#")[0].split("/");
    if (!(0, utils_1.isLatestCommonTypesVersionForFile)(versionAndFile[0], versionAndFile[1])) {
        errors.push({
            message: `Use the latest version ${utils_1.LATEST_VERSION_BY_COMMON_TYPES_FILENAME.get(versionAndFile[1])} of ${versionAndFile[1]}.`,
            path: path,
        });
    }
    return errors;
};
exports.latestVersionOfCommonTypesMustBeUsed = latestVersionOfCommonTypesMustBeUsed;
//# sourceMappingURL=latest-version-of-common-types-must-be-used.js.map