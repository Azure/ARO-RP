import { ISwaggerInventory } from "@microsoft.azure/openapi-validator-core";
declare type WalkCallBack = (path: string[], value: any, rootPath: string, root: any) => void;
export declare class SwaggerWalker {
    private inventory;
    constructor(inventory: ISwaggerInventory);
    private _walkOnDocuments;
    warkReferenced(paths: string[], current: string, cb: WalkCallBack): void;
    warkAll(paths: string[], cb: WalkCallBack): void;
    warkAllExcept(paths: string[], excepts: string[] | string, cb: WalkCallBack): void;
}
export {};
