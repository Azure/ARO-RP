"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getLazyBuilder = void 0;
const getLazyBuilder = (key, builder) => {
    const errKey = "_err_" + key;
    const listeners = new Map();
    return async (source) => {
        let val = source[key];
        if (val !== undefined) {
            return val;
        }
        const err = source[errKey];
        if (err !== undefined) {
            throw err;
        }
        let listener = listeners.get(source);
        if (listener !== undefined) {
            return new Promise((resolve, reject) => listener.push({ resolve, reject }));
        }
        listener = [];
        listeners.set(source, listener);
        try {
            val = await builder(source);
            // eslint-disable-next-line require-atomic-updates
            source[key] = val;
            for (const { resolve } of listener) {
                resolve(val);
            }
            return val;
        }
        catch (e) {
            source[errKey] = e;
            for (const { reject } of listener) {
                reject(e);
            }
            throw e;
        }
        finally {
            listeners.delete(source);
        }
    };
};
exports.getLazyBuilder = getLazyBuilder;
//# sourceMappingURL=lazyBuilder.js.map