"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFilePositionFromJsonPath = exports.jsonPathToPointer = exports.jsonPathToArray = void 0;
const openapi_tools_common_1 = require("@azure-tools/openapi-tools-common");
const json_pointer_1 = __importDefault(require("json-pointer"));
const jsonpath_plus_1 = require("jsonpath-plus");
const jsonPathToArray = (jsonPath) => {
    return jsonpath_plus_1.JSONPath.toPathArray(jsonPath);
};
exports.jsonPathToArray = jsonPathToArray;
const jsonPathToPointer = (jsonPath) => {
    return json_pointer_1.default.compile((0, exports.jsonPathToArray)(jsonPath).slice(1));
};
exports.jsonPathToPointer = jsonPathToPointer;
const getFilePositionFromJsonPath = (obj, jsonPath) => {
    if (!jsonPath) {
        return undefined;
    }
    const pathArr = (0, exports.jsonPathToArray)(jsonPath.substr(1));
    /*
     * when jsonPath='/providers/Microsoft.Provider/resource',
     * the split pathArr will be ['/providers/Microsoft','Provider/resource'].
     * Only in this case, these two elements in the array need to be composed together by '.'.
     * So restrict the condition to the path element ends with /providers/Microsoft.
     */
    const newPathArr = pathArr.slice(0);
    const index = newPathArr.findIndex((str) => str.includes("/providers/Microsoft"));
    if (index !== -1 &&
        newPathArr[index + 1] !== undefined &&
        newPathArr[index].slice(-20) === "/providers/Microsoft") {
        newPathArr[index] += "." + newPathArr[index + 1];
        newPathArr.splice(index + 1, 1);
    }
    try {
        const target = json_pointer_1.default.get(obj, json_pointer_1.default.compile(newPathArr));
        const info = (0, openapi_tools_common_1.getInfo)(target);
        if (info !== undefined) {
            return info.position;
        }
    }
    catch (e) {
        // Pass
    }
    const lastProperty = newPathArr.pop();
    const target = json_pointer_1.default.get(obj, json_pointer_1.default.compile(newPathArr));
    const info = (0, openapi_tools_common_1.getInfo)(target);
    if (info !== undefined && lastProperty !== undefined) {
        return info.primitiveProperties[lastProperty];
    }
    return undefined;
};
exports.getFilePositionFromJsonPath = getFilePositionFromJsonPath;
//# sourceMappingURL=jsonUtils.js.map