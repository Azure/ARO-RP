"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.discriminatorTransformer = void 0;
const openapi_tools_common_1 = require("@azure-tools/openapi-tools-common");
const liveValidator_1 = require("../liveValidation/liveValidator");
const jsonLoader_1 = require("../swagger/jsonLoader");
const swaggerTypes_1 = require("../swagger/swaggerTypes");
const constants_1 = require("../util/constants");
const allOfTransformer_1 = require("./allOfTransformer");
const context_1 = require("./context");
const transformer_1 = require("./transformer");
const getDiscriminatorRoot = (sch, visited, baseSchemas, jsonLoader) => {
    var _a;
    if (sch.discriminator !== undefined) {
        return (_a = sch[swaggerTypes_1.refSelfSymbol]) !== null && _a !== void 0 ? _a : null;
    }
    if (sch.allOf === undefined) {
        return null;
    }
    let root = visited.get(sch);
    if (root !== undefined) {
        return root;
    }
    visited.set(sch, null);
    for (let subSch of sch.allOf) {
        if (!(0, jsonLoader_1.isRefLike)(subSch)) {
            continue;
        }
        subSch = jsonLoader.resolveRefObj(subSch);
        root = getDiscriminatorRoot(subSch, visited, baseSchemas, jsonLoader);
        if (root !== null) {
            baseSchemas.add(subSch);
            visited.set(sch, root);
            return root;
        }
    }
    return null;
};
const getDiscriminatorValue = (sch) => {
    var _a;
    const discriminatorValue = (_a = sch[constants_1.xmsDiscriminatorValue]) !== null && _a !== void 0 ? _a : (0, context_1.getNameFromRef)(sch);
    if (discriminatorValue === undefined) {
        throw new Error("undefined discriminatorValue!");
    }
    return discriminatorValue;
};
exports.discriminatorTransformer = {
    type: transformer_1.TransformerType.Global,
    before: [allOfTransformer_1.allOfTransformer],
    transform({ objSchemas, baseSchemas, jsonLoader, logging }) {
        const visited = new Map();
        for (const sch of objSchemas) {
            try {
                const rootRef = getDiscriminatorRoot(sch, visited, baseSchemas, jsonLoader);
                if (rootRef === null) {
                    if (sch[constants_1.xmsDiscriminatorValue] !== undefined) {
                        sch._missingDiscriminator = true;
                    }
                    continue;
                }
                const baseSch = jsonLoader.resolveRefObj({ $ref: rootRef });
                const $ref = sch[swaggerTypes_1.refSelfSymbol];
                const discriminatorValue = getDiscriminatorValue(sch);
                if (baseSch.discriminatorMap === undefined) {
                    baseSch.discriminatorMap = {
                        [getDiscriminatorValue(baseSch)]: null,
                    };
                    (0, openapi_tools_common_1.copyInfo)(baseSch, baseSch.discriminatorMap);
                }
                baseSch.discriminatorMap[discriminatorValue] = { $ref };
            }
            catch (e) {
                if (logging) {
                    logging(`Fail to transform ${sch}. ErrorMessage:${e === null || e === void 0 ? void 0 : e.message};ErrorStack:${e === null || e === void 0 ? void 0 : e.stack}.`, liveValidator_1.LiveValidatorLoggingLevels.error);
                }
                else {
                    console.log(`Fail to transform ${sch}. ErrorMessage:${e === null || e === void 0 ? void 0 : e.message};ErrorStack:${e === null || e === void 0 ? void 0 : e.stack}.`);
                }
            }
        }
    },
};
//# sourceMappingURL=discriminatorTransformer.js.map