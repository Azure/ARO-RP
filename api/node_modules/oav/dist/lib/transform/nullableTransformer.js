"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.nullableTransformer = void 0;
const liveValidator_1 = require("../liveValidation/liveValidator");
const constants_1 = require("../util/constants");
const allOfTransformer_1 = require("./allOfTransformer");
const transformer_1 = require("./transformer");
exports.nullableTransformer = {
    type: transformer_1.TransformerType.Global,
    after: [allOfTransformer_1.allOfTransformer],
    transform({ objSchemas, allParams, arrSchemas, jsonLoader, logging }) {
        var _a;
        for (const sch of objSchemas) {
            try {
                if (sch.properties !== undefined) {
                    for (const key of Object.keys(sch.properties)) {
                        sch.properties[key] = transformNullable(sch.properties[key], jsonLoader, !((_a = sch.required) === null || _a === void 0 ? void 0 : _a.includes(key)));
                    }
                }
                const aProperty = sch.additionalProperties;
                if (typeof aProperty === "object" && aProperty !== null) {
                    sch.additionalProperties = transformNullable(aProperty, jsonLoader, undefined, aProperty.type === "object");
                }
            }
            catch (e) {
                if (logging) {
                    logging(`Fail to transform ${sch}. ErrorMessage:${e === null || e === void 0 ? void 0 : e.message};ErrorStack:${e === null || e === void 0 ? void 0 : e.stack}.`, liveValidator_1.LiveValidatorLoggingLevels.error);
                }
                else {
                    console.log(`Fail to transform ${sch}. ErrorMessage:${e === null || e === void 0 ? void 0 : e.message};ErrorStack:${e === null || e === void 0 ? void 0 : e.stack}.`);
                }
            }
        }
        for (const sch of arrSchemas) {
            try {
                if (sch.items) {
                    if (Array.isArray(sch.items)) {
                        sch.items = sch.items.map((item) => transformNullable(item, jsonLoader));
                    }
                    else {
                        sch.items = transformNullable(sch.items, jsonLoader);
                    }
                }
            }
            catch (e) {
                if (logging) {
                    logging(`Fail to transform ${sch}. ErrorMessage:${e === null || e === void 0 ? void 0 : e.message};ErrorStack:${e === null || e === void 0 ? void 0 : e.stack}.`, liveValidator_1.LiveValidatorLoggingLevels.error);
                }
                else {
                    console.log(`Fail to transform ${sch}. ErrorMessage:${e === null || e === void 0 ? void 0 : e.message};ErrorStack:${e === null || e === void 0 ? void 0 : e.stack}.`);
                }
            }
        }
        for (const param of allParams) {
            if (param.in === "query" && param.allowEmptyValue) {
                param.nullable = true;
            }
        }
    },
};
const transformNullable = (s, jsonLoader, defaultNullable, additionalPropertiesWithObjectType) => {
    var _a;
    const sch = jsonLoader.resolveRefObj(s);
    const nullable = (_a = sch[constants_1.xNullable]) !== null && _a !== void 0 ? _a : sch.nullable;
    // Originally it's not nullable
    if (nullable === false) {
        return s;
    }
    // By default it's not nullable
    if (nullable === undefined && defaultNullable === false) {
        return s;
    }
    // Set nullable to true
    if (s !== sch) {
        // s isRefLike
        return {
            anyOf: [s, { type: "null", _skipError: true }],
            _skipError: true,
        };
    }
    else {
        if (typeof sch === "object") {
            sch.nullable = true;
        }
        if (additionalPropertiesWithObjectType) {
            sch.additionalPropertiesWithObjectType = true;
        }
        return sch;
    }
};
//# sourceMappingURL=nullableTransformer.js.map