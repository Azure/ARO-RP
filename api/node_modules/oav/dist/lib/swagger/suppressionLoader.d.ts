import { FileLoader, FileLoaderOption } from "./fileLoader";
import { Loader } from "./loader";
import { SwaggerSpec } from "./swaggerTypes";
export interface SuppressionLoaderOption extends FileLoaderOption {
    loadSuppression?: string[];
}
export declare class SuppressionLoader implements Loader<void, SwaggerSpec> {
    private fileLoader;
    private suppressionCache;
    private suppressionToLoad;
    constructor(opts: SuppressionLoaderOption, fileLoader: FileLoader);
    load(spec: SwaggerSpec): Promise<void>;
    private getSuppression;
}
export declare const isSuppressed: (node: any, code: string, message: string) => boolean;
export declare const isSuppressedInPath: (node: any, code: string, message: string) => boolean;
//# sourceMappingURL=suppressionLoader.d.ts.map