import { Loader } from "./loader";
export interface FileLoaderOption {
    fileRoot?: string;
    checkUnderFileRoot?: boolean;
}
export declare class FileLoader implements Loader<string> {
    private opts;
    private preloadCache;
    constructor(opts: FileLoaderOption);
    load(filePath: string): Promise<string>;
    relativePath(filePath: string): string;
    resolvePath(filePath: string): string;
    isUnderFileRoot(filePath: string): boolean;
    writeFile(filePath: string, content: string): Promise<void>;
    appendFile(filePath: string, content: string): Promise<void>;
    preloadExtraFile(filePath: string, content: string): void;
}
//# sourceMappingURL=fileLoader.d.ts.map