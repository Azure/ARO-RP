"use strict";
// Copyright (c) 2021 Microsoft Corporation
//
// This software is released under the MIT License.
// https://opensource.org/licenses/MIT
Object.defineProperty(exports, "__esModule", { value: true });
exports.SchemaSearcher = void 0;
const jsonLoader_1 = require("../swagger/jsonLoader");
const logger_1 = require("./logger");
const diffUtils_1 = require("./diffUtils");
class SchemaSearcher {
    static findSchemaByJsonPointer(jsonPointer, schema, jsonLoader, body) {
        const steps = jsonPointer.split("/");
        let curSchema = schema;
        let rootSchema = schema;
        if ((0, jsonLoader_1.isRefLike)(schema)) {
            rootSchema = this.getProperties(schema, jsonLoader);
        }
        const curPaths = [];
        try {
            for (const step of steps) {
                curSchema = SchemaSearcher.getProperties(curSchema, jsonLoader);
                let found = false;
                if (step !== "") {
                    // if current step is array.
                    if (!isNaN(+step)) {
                        if (curSchema.type === "array") {
                            curSchema = curSchema.items;
                        }
                    }
                    if ((curSchema === null || curSchema === void 0 ? void 0 : curSchema.properties) !== undefined && (curSchema === null || curSchema === void 0 ? void 0 : curSchema.properties[step]) !== undefined) {
                        curSchema = curSchema.properties[step];
                        found = true;
                    }
                }
                curPaths.push(step);
                // If not found, find in discriminator.
                if (!found && rootSchema.discriminatorMap !== undefined) {
                    let discriminatorValue = "";
                    if (body !== undefined && rootSchema.discriminator !== undefined) {
                        const discriminatorJsonPointer = [...curPaths, rootSchema.discriminator].join("/");
                        discriminatorValue = (0, diffUtils_1.getObjValueFromPointer)(body, discriminatorJsonPointer);
                    }
                    if (discriminatorValue !== "") {
                        const discriminatorSchemaRef = rootSchema.discriminatorMap[discriminatorValue];
                        if (discriminatorSchemaRef !== undefined) {
                            const discriminatorSchema = SchemaSearcher.getProperties(discriminatorSchemaRef, jsonLoader);
                            curSchema = discriminatorSchema;
                            found = true;
                        }
                    }
                }
            }
            return this.getProperties(curSchema, jsonLoader);
        }
        catch (err) {
            logger_1.logger.error(err);
        }
    }
    static getProperties(schema, jsonLoader) {
        var _a, _b;
        let ret = {};
        if ((0, jsonLoader_1.isRefLike)(schema)) {
            ret = jsonLoader.resolveRefObj(schema);
        }
        (_a = schema.allOf) === null || _a === void 0 ? void 0 : _a.map((item) => {
            ret = {
                ...ret,
                ...this.getProperties(jsonLoader.resolveRefObj(item), jsonLoader),
            };
        });
        (_b = schema.anyOf) === null || _b === void 0 ? void 0 : _b.map((item) => {
            ret = {
                ...ret,
                ...this.getProperties(jsonLoader.resolveRefObj(item), jsonLoader),
            };
        });
        return {
            ...ret,
            ...schema,
        };
    }
}
exports.SchemaSearcher = SchemaSearcher;
//# sourceMappingURL=schemaSearcher.js.map