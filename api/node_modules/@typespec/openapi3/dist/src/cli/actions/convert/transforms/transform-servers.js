function transformServerVariables(variables) {
    if (!variables)
        return undefined;
    const result = {};
    for (const [name, variable] of Object.entries(variables)) {
        result[name] = {
            default: variable.default,
            description: variable.description,
            enum: variable.enum ? [...variable.enum] : undefined,
        };
    }
    return result;
}
export function transformServers(servers) {
    return servers.map((server) => ({
        url: server.url,
        description: server.description,
        variables: transformServerVariables(server.variables),
    }));
}
//# sourceMappingURL=transform-servers.js.map