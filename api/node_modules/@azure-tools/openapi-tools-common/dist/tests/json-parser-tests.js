"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert = __importStar(require("assert"));
const json_parser_1 = require("../libs/json-parser");
const source_map_1 = require("../libs/source-map");
const iterator_1 = require("../libs/iterator");
const fs = __importStar(require("fs"));
describe("parse", () => {
    it("empty", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "", e => errors.push(e));
        assert.strictEqual(json, null);
        assert.equal(errors.length, 1);
        const x = errors[0];
        assert.equal(x.code, "unexpected end of file");
        assert.equal(x.message, "unexpected end of file, token: , line: 1, column: 1");
    });
    it("null", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "null", e => errors.push(e));
        assert.strictEqual(json, null);
        assert.equal(errors.length, 0);
    });
    it("number", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "+234.56e-1", e => errors.push(e));
        assert.equal(json, 23.456);
        assert.equal(errors.length, 0);
    });
    it("string", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "  \"hello world!\"  ", e => errors.push(e));
        assert.equal(json, "hello world!");
        assert.equal(errors.length, 0);
    });
    it("empty object", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "  { \n }  ", e => errors.push(e));
        assert.deepEqual(json, {});
        assert.equal(errors.length, 0);
    });
    it("empty array", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "  [ \n    \n\t  ]  ", e => errors.push(e));
        assert.deepEqual(json, []);
        assert.equal(errors.length, 0);
    });
    it("object with one property", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "  { \"x\": 2\n }  ", e => errors.push(e));
        if (json === null || typeof json !== "object") {
            throw new Error("not object");
        }
        const info = source_map_1.getInfo(json);
        if (info === undefined || info.isChild) {
            throw new Error("info");
        }
        assert.equal(info.position.line, 1);
        assert.equal(info.position.column, 3);
        assert.equal(info.url, "fakeurl.json");
        assert.deepEqual(json, { x: 2 });
        assert.equal(errors.length, 0);
    });
    it("object with three properties", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "  { \"x\": 2\n, \"\": true, \"rrr\":\n\n\n \t[] }  ", e => errors.push(e));
        const jsonRrr = json.rrr;
        const info = source_map_1.getInfo(jsonRrr);
        if (info === undefined || !info.isChild) {
            throw new Error("info");
        }
        assert.equal(info.position.line, 5);
        assert.equal(info.position.column, 3);
        assert.equal(info.property, "rrr");
        const parentInfo = info.parent[source_map_1.objectInfoSymbol]();
        if (parentInfo.isChild) {
            throw new Error("info");
        }
        assert.deepEqual(json, { x: 2, "": true, rrr: [] });
        assert.equal(errors.length, 0);
    });
    it("array with one item", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "  [ false ]  ", e => errors.push(e));
        assert.deepEqual(json, [false]);
        assert.equal(errors.length, 0);
    });
    it("array with three items", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", "  [ false, { \"na::\": [ null, true] }, -456 ]  ", e => errors.push(e));
        assert.deepEqual(json, [false, { "na::": [null, true] }, -456]);
        assert.equal(errors.length, 0);
        const na = json[1]["na::"];
        const info = source_map_1.getInfo(na);
        if (info === undefined || !info.isChild) {
            throw new Error("info");
        }
        assert.equal(info.property, "na::");
    });
    it("two values", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " false true ", e => errors.push(e));
        assert.deepEqual(json, false);
        assert.equal(errors.length, 1);
        const error = errors[0];
        assert.equal(error.code, "unexpected token");
        assert.equal(error.message, "unexpected token, token: true, line: 1, column: 8");
    });
    it("two tokens after value", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " {} [] ", e => errors.push(e));
        assert.deepEqual(json, {});
        assert.equal(errors.length, 1);
    });
    it("invalid second property", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " { \"\": 4 5 }", e => errors.push(e));
        assert.deepEqual(json, { "": 4 });
        assert.equal(errors.length, 1);
    });
    it("invalid property separator", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " { \"\" 4 }", e => errors.push(e));
        assert.deepEqual(json, {});
        assert.strictEqual(errors.length > 0, true);
    });
    it("invalid property name", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " { [] }", e => errors.push(e));
        assert.deepEqual(json, {});
        assert.strictEqual(errors.length > 0, true);
    });
    it("strange property name", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " { 45:54 }", e => errors.push(e));
        assert.deepEqual(json, { "45": 54 });
        assert.equal(errors.length, 1);
    });
    it("null property name", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " { null:54 }", e => errors.push(e));
        assert.deepEqual(json, { "null": 54 });
        assert.equal(errors.length, 1);
    });
    it("array with no separator", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " [ null \n +567.4e-56]", e => errors.push(e));
        assert.deepEqual(json, [null]);
        assert.equal(errors.length, 1);
    });
    it("invalid json", () => {
        const errors = [];
        const json = json_parser_1.parseJson("fakeurl.json", " } []", e => errors.push(e));
        assert.deepEqual(json, []);
        assert.equal(errors.length, 1);
    });
    it("testCase", () => {
        const url = "./src/tests/json-parser-testdata.json";
        const context = fs.readFileSync(url).toString();
        json_parser_1.parseJson(url, context, e => { throw e; });
    });
});
describe("tokenize", () => {
    it("empty", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("", e => errors.push(e), "url"));
        assert.deepStrictEqual(result, []);
        assert.equal(errors.length, 0);
    });
    it("spaces", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("   \t\n   ", e => errors.push(e), "url"));
        assert.deepStrictEqual(result, []);
        assert.equal(errors.length, 0);
    });
    it("string", () => {
        const errors = [];
        const ir = json_parser_1.tokenize(" \"xxx\"   ", e => errors.push(e), "url");
        const result = iterator_1.toArray(ir);
        assert.equal(result.length, 1);
        const token = result[0];
        if (token.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token.value, "xxx");
        assert.equal(token.position.line, 1);
        assert.equal(token.position.column, 2);
        assert.equal(errors.length, 0);
        return;
    });
    it("stringEscape", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize(" \n  \"xx\\\"x\"   ", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token = result[0];
        if (token.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token.value, "xx\"x");
        assert.equal(token.position.line, 2);
        assert.equal(token.position.column, 3);
        assert.equal(errors.length, 0);
        return;
    });
    it("stringUnicodeEscape", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("\"\\u00AE\"", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token = result[0];
        if (token.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token.value, "®");
        assert.equal(token.position.line, 1);
        assert.equal(token.position.column, 1);
        assert.equal(errors.length, 0);
        return;
    });
    it("invalidUnicodeEscape", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("\"\\u0XAE\"", e => errors.push(e), "someurl.json"));
        assert.equal(result.length, 1);
        const token = result[0];
        if (token.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token.value, "AE");
        assert.equal(token.position.line, 1);
        assert.equal(token.position.column, 1);
        assert.equal(errors.length, 1);
        assert.deepStrictEqual(errors[0].url, "someurl.json");
        return;
    });
    it("unexpectedUnicodeEscapeEnd", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("\"\\u0", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token = result[0];
        if (token.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token.value, "");
        assert.equal(token.position.line, 1);
        assert.equal(token.position.column, 1);
        assert.equal(errors.length, 2);
        return;
    });
    it("symbol", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize(" \r\n\t  {   ", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token = result[0];
        assert.equal(token.kind, "{");
        assert.equal(token.position.line, 2);
        assert.equal(token.position.column, 4);
        assert.equal(errors.length, 0);
    });
    it("true and false", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize(" \r\n\n\t   true  false ", e => errors.push(e), "url"));
        assert.equal(result.length, 2);
        const token0 = result[0];
        if (token0.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token0.position.line, 3);
        assert.equal(token0.position.column, 5);
        assert.strictEqual(token0.value, true);
        const token1 = result[1];
        if (token1.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token1.position.line, 3);
        assert.equal(token1.position.column, 11);
        assert.strictEqual(token1.value, false);
        assert.equal(errors.length, 0);
        return;
    });
    it("symbol and numbers", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("-234,56.78", e => errors.push(e), "url"));
        assert.equal(result.length, 3);
        const token0 = result[0];
        if (token0.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token0.position.line, 1);
        assert.equal(token0.position.column, 1);
        assert.equal(token0.value, -234);
        const token1 = result[1];
        assert.equal(token1.kind, ",");
        assert.equal(token1.position.line, 1);
        assert.equal(token1.position.column, 5);
        const token2 = result[2];
        if (token2.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token2.position.line, 1);
        assert.equal(token2.position.column, 6);
        assert.equal(token2.value, 56.78);
        assert.equal(errors.length, 0);
        return;
    });
    it("null and string", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("null\"-234\"", e => errors.push(e), "url"));
        assert.equal(result.length, 2);
        const token0 = result[0];
        if (token0.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token0.position.line, 1);
        assert.equal(token0.position.column, 1);
        assert.strictEqual(token0.value, null);
        const token1 = result[1];
        if (token1.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token1.position.line, 1);
        assert.equal(token1.position.column, 5);
        assert.equal(token1.value, "-234");
        assert.equal(errors.length, 0);
        return;
    });
    it("invalid number", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("-+123e+56", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token0 = result[0];
        if (token0.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token0.position.line, 1);
        assert.equal(token0.position.column, 1);
        assert.equal(token0.value, "-+123e+56");
        assert.equal(errors.length, 1);
        return;
    });
    it("control character", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("\"\n\"", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token0 = result[0];
        if (token0.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token0.position.line, 1);
        assert.equal(token0.position.column, 1);
        assert.equal(token0.value, "\n");
        assert.equal(errors.length, 1);
        return;
    });
    it("invalid escape", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("\"\\a\"", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token0 = result[0];
        if (token0.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token0.position.line, 1);
        assert.equal(token0.position.column, 1);
        assert.equal(token0.value, "a");
        assert.equal(errors.length, 1);
        return;
    });
    it("end of file", () => {
        const errors = [];
        const result = iterator_1.toArray(json_parser_1.tokenize("\"xyz", e => errors.push(e), "url"));
        assert.equal(result.length, 1);
        const token0 = result[0];
        if (token0.kind !== "value") {
            return assert.fail();
        }
        assert.equal(token0.position.line, 1);
        assert.equal(token0.position.column, 1);
        assert.equal(token0.value, "xyz");
        assert.equal(errors.length, 1);
        return;
    });
    it("invalid symbol", () => {
        const errors = [];
        iterator_1.toArray(json_parser_1.tokenize("*", e => errors.push(e), "url"));
        assert.equal(errors.length, 1);
    });
});
//# sourceMappingURL=json-parser-tests.js.map