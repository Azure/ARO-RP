import { useStateMap } from "@typespec/compiler/utils";
import { AzureCoreStateKeys } from "../lib.js";
export const [getPollingOperationParameter, setPollingOperationParameter] = useStateMap(AzureCoreStateKeys.pollingOperationParameter);
export const $pollingOperationParameter = (context, entity, target) => {
    const { program } = context;
    let storedValue;
    switch (target?.kind) {
        case "ModelProperty":
            storedValue = target;
            break;
        case "String":
            storedValue = target.value;
            break;
        default:
            storedValue = undefined;
    }
    setPollingOperationParameter(program, entity, storedValue ?? entity.name);
};
//# sourceMappingURL=polling-operation-parameter.js.map