import type { Model, Type } from "@typespec/compiler";
export interface EmbeddingVectorMetadata {
    elementType: Type;
}
export declare const 
/**
 * If the provided model is an embedding vector, returns the appropriate metadata; otherwise,
 * returns undefined.
 * @param model the model to query
 * @returns `EmbeddingVectorMetadata`, if applicable, or undefined.
 */
getAsEmbeddingVector: (program: import("@typespec/compiler").Program, type: Model) => EmbeddingVectorMetadata | undefined, setAsEmbeddingVector: (program: import("@typespec/compiler").Program, type: Model, value: EmbeddingVectorMetadata) => void;
//# sourceMappingURL=embedding-vector.d.ts.map