import { compilerAssert } from "@typespec/compiler";
import { useStateMap } from "@typespec/compiler/utils";
import { AzureCoreStateKeys } from "../../lib.js";
const [getParameterizedNextLinkArguments, markParameterizedNextLinkConfigTemplate] = useStateMap(AzureCoreStateKeys.parameterizedNextLinkConfig);
const parameterizedNextLinkConfigDecorator = (context, target, parameters) => {
    // Workaround as it seems like decorators are called when missing template arguments
    if (parameters.kind === "Model")
        return;
    compilerAssert(parameters.kind === "Tuple", "Using the defined internal scalar parameterizedNextLink will result in a Tuple template argument type");
    markParameterizedNextLinkConfigTemplate(context.program, target, parameters.values);
};
export { getParameterizedNextLinkArguments, parameterizedNextLinkConfigDecorator };
//# sourceMappingURL=parameterized-next-link-config.js.map